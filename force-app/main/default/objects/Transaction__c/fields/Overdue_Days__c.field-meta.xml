<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Overdue_Days__c</fullName>
    <externalId>false</externalId>
    <formula>IF(AND(ISBLANK(Return_Date__c), TODAY() &gt; Due_Date__c), TODAY() - Due_Date__c, IF(AND(NOT(ISBLANK(Return_Date__c)), Return_Date__c &gt; Due_Date__c), Return_Date__c - Due_Date__c, 0))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Overdue Days</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
